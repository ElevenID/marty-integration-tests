enabledFeatures = [
    auth,  # Enable legacy auth for /wallet-api/auth/* endpoints
    registration-defaults,
    key-generation-defaults,
    openapi,
    did-web-registry
]
disabledFeatures = [
    dev-mode  # Disable dev mode for production-like testing
]

